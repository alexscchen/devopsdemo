node {
    checkout scm

    docker.withRegistry('https://registry.hub.docker.com', 'github-id') {

        def customImage = docker.build("alexscchen/devopsdemo")

        /* Push the container to the custom Registry */
        customImage.push()
    }
}
